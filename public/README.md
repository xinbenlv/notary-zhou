# Public assets

Everything here is served publicly. Keep credentials and private reporting data out. `images/` contains brand and article assets; `robots.txt` advertises the sitemap index. `mockups/` contains existing interface previews. Source documentation in README files is deliberately brief and contains no private material.
